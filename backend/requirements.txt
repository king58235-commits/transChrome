websockets>=12.0
faster-whisper>=1.0.0
# GPU runtime for ctranslate2/faster-whisper on Windows without a full CUDA
# Toolkit install — see transcriber.py's _register_nvidia_dll_dirs().
nvidia-cublas-cu12
nvidia-cudnn-cu12
